<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">


    <group android:checkableBehavior="single">
        <item
            android:id="@+id/home_menu"
            android:icon="@drawable/home_menu"
            android:title="Home" />

        <item
            android:id="@+id/myDetails_menu"
            android:icon="@drawable/details_menu"
            android:title="My Details" />

        <item
            android:id="@+id/dept_menu"
            android:icon="@drawable/dept_menu"
            android:title="My Department" />
    </group>

    <item android:title="Settings">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/settings_menu"
                    android:icon="@drawable/settings_menu"
                    android:title="Settings" />
            </group>
        </menu>
    </item>


    <item android:title="Authentication">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/login_menu"
                    android:icon="@drawable/login_menu"
                    android:title="Log In" />

                <item
                    android:id="@+id/logout_menu"
                    android:icon="@drawable/logout"
                    android:title="Log Out" />
            </group>
        </menu>
    </item>

</menu>